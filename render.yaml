services:
  - type: web
    name: discord-verification
    env: python
    region: virginia
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: |
      python run.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9  # Downgrade to 3.11 for compatibility
      - key: FLASK_ENV
        value: production
    healthCheckPath: /health
    autoDeploy: true